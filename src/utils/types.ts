export type PeriodicElement = {
  fullName: string;
  symbol: string;
  atomicNumber: number;
  atomicMass: number;
};

export type LocaleStrings = {
  title: string;
  subtitle: string;
  button: string;
  placeholder: string;
  titles: {
    input: string;
    output: string;
  };
};
